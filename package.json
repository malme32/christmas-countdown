{
  "name": "pacman-web-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static HTML/CSS/JS + Canvas Pacman game with a testable core.",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=18"
  }
}
